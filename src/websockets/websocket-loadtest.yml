config:
  target: "ws://35.225.171.95/websocket"
  phases:
    - duration: 640
      arrivalRate: 100
  ws:
    # Ignore SSL certificate errors
    # - useful in *development* with self-signed certs
    rejectUnauthorized: false
scenarios:
  - engine: "ws"
    flow:
      - send: "ping"
